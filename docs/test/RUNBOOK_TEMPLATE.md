# Test Runbook Template

本文件是 `docs/test/*` 的通用 runbook 模板，用于承接可复用验证步骤和提交版脱敏结果摘要。复制成具体测试文档后，先替换占位内容，再按真实执行结果维护。

## 当前验证结果

- 记录时间：
- 记录目录：
- 本轮任务性质：
- 当前结论：`未执行`
- 自动化入口：
- 对应计划 / issue：
- 结果说明：

### 本次执行结果

- 执行时间：
- 执行目录：
- 本次结论：`未执行`
- 影响范围：
- 清理结果：
- 敏感信息处理：未写入真实凭据、token、cookie、数据库主机、连接串、行主键、临时目录、完整下载 URL、原始响应或其它机器本地痕迹。

### 当前步骤状态

| 步骤 | 结果 | 备注 |
| --- | --- | --- |
| 前置检查 | 未执行 |  |
| 主路径验证 | 未执行 |  |
| 清理 | 未执行 |  |

## 目标

- 验证目标：
- 成功标准：
- 本 runbook 是给 agent 或工程师直接执行的步骤文档，不是泛化 QA 说明。

## 执行副作用

- 可能写入的本地文件：
- 可能访问的服务 / 数据库 / 外部系统：
- 可能创建的临时数据：
- 明确不会触达的范围：
- 执行前必须先说明副作用和影响范围；如果影响范围不清楚，先停止确认。

## 前置条件

1. 当前工作目录：
2. 当前分支或版本：
3. 必需命令：
4. 必需配置：
5. 必需测试环境：

## 测试变量 / 初始化

```bash
set -euo pipefail

REPO_ROOT="${REPO_ROOT:-/absolute/path/to/repo}"
RUN_ID="${RUN_ID:-test-run-$(date +%Y%m%d%H%M%S)}"

cd "$REPO_ROOT"

echo "REPO_ROOT=$REPO_ROOT"
echo "RUN_ID=$RUN_ID"
```

预期结果：

- 初始化命令成功退出。
- 关键变量已打印或确认，但不包含敏感值。

## 主路径

### 1. 前置检查

```bash
# 写入真实前置检查命令
```

预期结果：

- 

### 2. 执行验证

```bash
# 写入真实验证命令
```

预期结果：

- 

### 3. 清理

```bash
# 写入真实清理命令；无清理动作时说明原因
```

预期结果：

- 清理结果可验证。

## 失败处理

| 失败点 | 停止条件 | 记录方式 | 恢复 / 重跑方式 |
| --- | --- | --- | --- |
| 前置检查失败 | 停止 | 记录 blocker | 修复前置条件后重跑 |
| 主路径失败 | 停止 | 记录失败步骤与脱敏摘要 | 按恢复步骤重跑 |
| 清理失败 | 停止并升级 | 记录残留范围 | 人工清理后再继续 |

## 结果回写

执行完成后，回写本文前部的 `当前验证结果`、`本次执行结果` 和 `当前步骤状态`。

固定规则：

- 已执行的步骤写真实结果。
- 未执行的步骤显式写 `未执行` 或 `blocker`，不得写成 `通过`。
- 提交版文档只保留脱敏摘要，不写真实凭据、token、cookie、数据库主机、连接串、行主键、临时目录、完整下载 URL、原始响应或其它机器本地痕迹。
- 原始命令输出、敏感信息和机器本地痕迹只放在受控的本地记录、issue comment 或 `.agents/runs`，不写入提交版文档。
- 后续同步或 closeout 不得把已脱敏的历史结果摘要删成空模板；有新结果时，用新的脱敏摘要替换或追加。
